/// Template for env.dart — copy this file, rename to env.dart, fill in keys.
/// env.dart is gitignored and must never be committed.
class Env {
  // Gemini API — aistudio.google.com/app/apikey
  static const geminiApiKey = 'YOUR_GEMINI_API_KEY';

  // Shotstack — shotstack.io/dashboard
  static const shotstackApiKey = 'YOUR_SHOTSTACK_API_KEY';
  static const shotstackOwnerId = 'YOUR_SHOTSTACK_OWNER_ID';
  static const shotstackEnv = 'stage'; // 'stage' | 'v1'
}
